# Core data processing
polars[rt64]
pyarrow  # Parquet file support
pandas
numpy
endplay  # Bridge library (Deal, Vul, Player, dds, parsers)

# GPU acceleration (optional - install with: pip install torch --index-url https://download.pytorch.org/whl/cu124)
# torch

# Progress bars
tqdm

# Notebook support
IPython
xlsxwriter

# API server
fastapi
uvicorn[standard]
pydantic
psutil

# Streamlit app
streamlit
streamlit-aggrid==1.1.8.post1
requests

# Database
duckdb

# Standard library (included with Python, no install needed):
# pathlib, multiprocessing, pickle, re, gc, time, sys, datetime,
# collections, operator, threading, importlib, random, typing
